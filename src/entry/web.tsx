/**
 * This is the entry point for the
 * extension
 */
import React from 'react'
import ReactDOM from 'react-dom'
import { WebBootstrap } from '../js/App'

ReactDOM.render(<WebBootstrap />, document.getElementById('root'))
