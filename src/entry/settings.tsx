/**
 * This is the entry point for the settings
 * popup
 */
import React from 'react'
import ReactDOM from 'react-dom'
import { SettingsBoostrap } from '../js/App'

ReactDOM.render(<SettingsBoostrap />, document.getElementById('root'))
